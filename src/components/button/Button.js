import styled from 'styled-components'

const Button = styled.button`
    background: #00e526;
    border: none;
    border-radius: 3px;
    color: #FDFDFD;
    font-size: 1.2rem;
    margin: 1rem;
    padding: 1rem 1.5rem;

    &:hover {
      cursor: pointer;
    }

`

export default Button;